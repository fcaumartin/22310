{% extends 'base.html.twig' %}


{% block body %}




    <div class="col-12">
        <h1>{{produit.libelle}}</h1>
    </div>

    <img src="{{produit.image}}" alt="" srcset="" class="w-50">

    <div class="col-12">
        {{produit.description}}
    </div>

    <div class="col-12">
        <a href="/add/{{produit.id}}" class="btn btn-primary">Ajouter au panier</a>
    </div>

{% endblock %}
